<p id="notice"><%= notice %></p>



  <section class="section">
    <div class="container">
      <div class="columns">
      <%= render 'trend'%>
      <%= render 'feed'%>
      <%= render 'who-to-follow'%>
      </div>
    </div>
  </section>
